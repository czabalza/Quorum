<% question.tags().each(function (tag) { %>
  <div>
    <%= tag.escape("topic") %>
  </div>
<% }) %>
<a href=<%= "#questions/" + question.id %>><%= question.escape('title') %></a>
<br>
<div id='new-answer-on-q-index'>
  <button class="new-answer-btn">Write Answer</button>
</div>
